= Особенности SQL- и Windows-аутентификации в БД

При создании новой БД {dv} и подключении существующей БД администратор может выбрать тип проверки подлинности: SQL-аутентификация или аутентификация Windows.

.Настройки подключения базы данных в Консоли настройки {dv}
image::AttachDbWindowsAccount.png[Настройки подключения базы данных в Консоли настройки {dv}]

.При выборе SQL-аутентификации:
* Создание и подключение БД будет выполнено от имени учетной записи из поля `Логин` с паролем из поля `Пароль`.
* Сервисы {dv} (кроме СУБП) будут подключаться к БД с использованием учетной записи с указанными данными.

.При выборе аутентификации Windows:
* Создание и подключение БД будет выполнено от учетной записи пользователя, запустившего _{cns}_.
* Сервисы {dv} (кроме СУБП) будут подключаться к БД с использованием учетных записей, от имени которых данные сервисы запущены.
* Сервисам сервера {dv} -- пулу приложений _DefaultAppPool_ и службе *{sss}* -- будет автоматически назначена учетная запись из поля `Логин` с паролем из поля `Пароль`.

Если планируется подключать xref:storage.adoc[внешнее хранилище] _Filestream хранилище в базе данных {dv}_, БД {dv} должна быть подключена с использованием аутентификации Windows.

Учетная запись, от имени которой создаётся или подключается БД, должна обладать следующими правами в СУБД:

include::installguide:ROOT:partial$dbRights.adoc[]

Перечень прав, необходимых для работы служб xref:ROOT:requirements-server-account.adoc[сервера {dv}], сервиса xref:ROOT:requirements-full-text-account.adoc[полнотекстового поиска] и xref:ROOT:requirements-file-service-account.adoc[файлового сервиса] смотрите в соответствующих пунктах.
