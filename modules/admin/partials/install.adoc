// tag::install[]
. Установите серверные компоненты {of} следующей командой, предварительно обновив индекс пакетов:
// tag::commands[]
+
// tag::commands-no-plus[]
[tabs]
====
{os1}::
+
[source,bash,subs=attributes]
----
sudo apt-get update
sudo apt-get install docsvision-{wt}
----

{os2}::
+
[source,bash,subs=attributes]
----
sudo dnf makecache
sudo dnf install docsvision-{wt}
----
====
// end::commands-no-plus[]
// end::commands[]
// end::install[]
+
ifndef::no-config[]
+
. {blank}
+
Приоритетным способом настройки системы является {mc}, настройки в конфигурационном файле могут быть упразднены в будущих версиях. Рекомендуется обновить систему до версии с Консолью управления {mc-cu-mc}.

endif::no-config[]