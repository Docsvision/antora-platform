= Особенности SQL- и Windows-аутентификации в БД

При создании новой БД {dv} и подключении существующей БД администратор может выбрать тип проверки подлинности: SQL-аутентификация или аутентификация Windows.

****
Ниже описывается настройка аутентификации в БД при помощи программы {cns}. Настройке аутентификации в БД при помощи {of-mc} не поддерживается. {mc} поддерживает только использование SQL-аутентификации.
****

.Настройки подключения базы данных в Консоли настройки {dv}
image::admin:db-create-parameters.png[Настройки подключения базы данных в Консоли настройки {dv}]

.При выборе SQL-аутентификации:
* Создание и подключение БД будет выполнено от имени учётной записи из поля `Логин` с паролем из поля `Пароль`.
* Сервисы {dv} (кроме СУБП) будут подключаться к БД с использованием учётной записи с указанными данными.

.При выборе аутентификации Windows:
* Создание и подключение БД будет выполнено от учётной записи пользователя, запустившего _{cns}_.
* Сервисы {dv} (кроме СУБП) будут подключаться к БД с использованием учётных записей, от имени которых данные сервисы запущены.
* Сервисам сервера {dv} -- пулу приложений _DefaultAppPool_ и службе *{sss-new}* -- будет автоматически назначена учётная запись из поля `Логин` с паролем из поля `Пароль`.

Если планируется подключать xref:storage.adoc[внешнее хранилище] _FileStream хранилище в базе данных {dv}_, БД {dv} должна быть подключена с использованием аутентификации Windows.

[WARNING]
====
include::admin:partial$excerpts.adoc[tags=nofs]
====

Учётная запись, от имени которой создаётся или подключается БД, должна обладать следующими правами в СУБД:

include::install-linux:ROOT:partial$dbRights.adoc[]

Перечень прав, необходимых для работы служб xref:ROOT:requirements-server-account.adoc[сервера {dv}], сервиса xref:ROOT:requirements-full-text-account.adoc[полнотекстового поиска] и xref:ROOT:requirements-file-service-account.adoc[файлового сервиса] смотрите в соответствующих пунктах.
